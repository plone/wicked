================
Backlink Manager
================

alot of setup here...gotta interact with alot of nasty old zope

    >>> from zope.app.tests.placelesssetup import setUp, tearDown
    >>> import Products.Five
    >>> from Products.Five import zcml
    >>> setUp()
    >>> zcml.load_config('meta.zcml', Products.Five)
    >>> zcml.load_config("permissions.zcml", Products.Five)
    >>> zcml.load_config('configure.zcml', Products.wicked.lib)
    >>> 
    >>> from Products.wicked.lib.testing import getToolByName
    >>> from Products.wicked.lib.factories import BacklinkManager
    >>> from Products.wicked.example.ironicwiki import IronicWiki
    >>> doc = IronicWiki('ironic')




